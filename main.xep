struct _file_t {
    let fhd: *byte;
    let bufsiz: int;
    let flag: int;
};

__builtin fun fopen(path: *char) => *_file_t;
__builtin fun fclose(fp: *_file_t) => int;
__builtin fun fread(fp: *_file_t, buf: *char, pos: int, size: int) => int;